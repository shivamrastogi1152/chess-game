class Piece {
  constructor(x, y, imgSrc, pieceType, team) {
    this.x = x;
    this.y = y;
    this.imgSrc = imgSrc;
    this.pieceType = pieceType;
    this.team = team;
  }
}

export default Piece;
